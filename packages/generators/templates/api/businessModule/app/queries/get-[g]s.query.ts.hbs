import type { FindAll{{pascalCase moduleName}}VO } from '../vo'
import type { FindAllDTO } from '@/common/dto'
import { Query } from '@nestjs/cqrs'

/** 获取{{zhName}}列表Query */
export class Get{{pascalCase moduleName}}sQuery extends Query<FindAll{{pascalCase moduleName}}VO> {
  constructor(public readonly findAllDTO: FindAllDTO) {
    super()
  }
}
